generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model DatabaseConnectionProbe {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())

  @@map("_boletas_database_connection_probe")
}
